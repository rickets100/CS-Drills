'use strict';
const push = require('./doubly_linked_list').push
const shift = require('./doubly_linked_list').shift

// Implement a queue using a doubly-linked list with a head/tail pointer
// Make enqueue(), dequeue() and size() all run in O(1) time

// See test/queue.test.js, this is a constructor function which should return a Queue
module.exports = function Queue() {

}
